##
## QAI Hub Models Nightly Tests
##
## DISABLED: This workflow has been disabled by the repository owner
##

name: QAI Hub Models Nightly

on:
  # DISABLED: Scheduled runs have been commented out
  # schedule:
  #     - cron: '0 6 * * 1-5' # Daily except Fri/Sat nights, at 10 PM PST (11 PM PDT)

  workflow_dispatch:
    inputs:
      models:
        description: Models to Run
        required: false
        type: string
        default: pytorch

      run_sc_integration_test:
        description: Run Scorecard Integration Test
        type: boolean
        default: true

      run_website_test:
        description: Run Website Import Test
        type: boolean
        default: true

jobs:
  disabled_notice:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow has been disabled by the repository owner"
